{
  "name": "ascend",
  "version": "1.0.0",
  "description": "Ascend — an agentic AI curator for personal growth and self-transformation",
  "type": "commonjs",
  "main": "server.js",
  "scripts": {
    "start": "node --env-file-if-exists=.env --experimental-sqlite server.js",
    "start:env": "node --env-file-if-exists=.env --experimental-sqlite server.js",
    "dev": "node --watch --env-file-if-exists=.env --experimental-sqlite server.js"
  },
  "engines": {
    "node": ">=22.5.0"
  },
  "dependencies": {
    "express": "^5.2.1"
  }
}
